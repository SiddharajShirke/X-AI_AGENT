# Antigravity workspace

Keep root `AGENTS.md` in context and select the matching skill under `antigravity/skills/`. The hidden `.antigravity/skills/` mirror is included for workspace conventions that expect a hidden folder.
