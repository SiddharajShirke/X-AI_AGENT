# Antigravity workspace notes

Use root `AGENTS.md` as the invariant contract. Select exactly one component skill for a focused task, then verify with its commands. Do not duplicate startup configuration in workspace instructions; keep it dynamic in the database and dashboard.
