services:
  x-agent:
    build: .
    container_name: startup-x-agent-prototype
    env_file:
      - .env
    environment:
      DATABASE_PATH: /data/x_agent.db
    ports:
      - "8000:8000"
    volumes:
      - x_agent_data:/data
    restart: unless-stopped

volumes:
  x_agent_data:
