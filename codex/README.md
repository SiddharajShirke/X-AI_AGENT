# Codex workspace

Start Codex from the repository root so root `AGENTS.md` is loaded. Before a focused task, ask Codex to read `codex/skills/<task>/SKILL.md`.

Example:

```text
Read AGENTS.md and codex/skills/approval-flow/SKILL.md, then add a reviewer identity field to dashboard approvals with tests first.
```
